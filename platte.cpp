//
// Created by sia on 9-8-18.
//
/*

#include "vektor.h"
#include <cmath>
//voll Objektorientiert
// ohne Objekte

platte::platte() {
    // Default Konstuktor .. alles Null
    v[0] = 0.;
    v[1] = 0.;
    v[2] = 0.;
}

platte::platte(vektor a, vektor b, vektor c, double m, double l, double h) {
   platte P =
}

double* vektor::get() {
    // getter
    return v;
}

double vektor::mag() {
    double mag = sqrt(v[0] * v[0] + v[1] * v[1] + v[2] * v[2]);
    return mag;
}*/